import BookingDetail from "../features/bookings/BookingDetail";

//rule here is for a page not to do fetching / side effects so the file structure to be cleaner

function Booking() {
  return <BookingDetail />;
}

export default Booking;
